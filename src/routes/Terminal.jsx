/**
 * Terminal route simulates a Unix-like "command line" interface.
 * Allows users to input commands and view outputs.
 */
const Terminal = () => {
    return (
        <div>
            <h1 class="text-xl">Terminal's page</h1>
        </div>
    )
}

export default Terminal
